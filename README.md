# rntme

Monorepo for rntme — a typed read-side query language and its compilers.

## Packages

- `packages/graph-ir-compiler` — Graph IR → SQL compiler (SQLite target, MVP Tier 1).

## Requirements

- Node.js ≥ 20
- pnpm ≥ 9

## Setup

```bash
pnpm install
```
