version: '3.8'
services:
  minio:
    image: minio/minio:latest
    command: server /data
    environment:
      MINIO_ROOT_USER: test
      MINIO_ROOT_PASSWORD: testtest
    ports:
      - "9000:9000"
  registry:
    image: registry:2
    ports:
      - "5000:5000"
