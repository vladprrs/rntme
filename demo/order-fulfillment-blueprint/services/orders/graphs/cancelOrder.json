{
  "id": "cancelOrder",
  "signature": {
    "inputs": {
      "orderId": { "type": "string", "mode": "required" },
      "reason": { "type": "string", "mode": "required" }
    },
    "output": { "type": "row<CommandResult>", "from": "emit" }
  },
  "nodes": [
    {
      "id": "emit",
      "type": "emit",
      "config": {
        "aggregate": "Order",
        "aggregateId": { "$param": "orderId" },
        "transition": "cancel",
        "payload": {
          "cancelReason": { "$param": "reason" }
        }
      }
    }
  ]
}
