# Copy this file into a directory that contains your artifacts (pdm.json,
# qsm.json, bindings.json, shapes.json, ui.json, manifest.json, graphs/*.json)
# and run:
#   docker build -t myorg/my-service:VERSION .

FROM ghcr.io/vladprrs/rntme-runtime:1.0
COPY . /srv/artifacts
